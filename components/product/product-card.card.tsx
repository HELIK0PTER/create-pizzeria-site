          {!extendedProduct.isAvailable && (
            <div className="absolute inset-0 flex items-center justify-center z-10">
              <span className="text-black text-3xl font-extrabold uppercase tracking-wider">
                Indisponible
              </span>
            </div>
          )}
        </div> 